package array;

public class ArrayDocs {
	/*
	 * ----------------------- Array ---------------------------------
	 *    # collection of homogeneous data.
	 *    # collection/group of multiple values of same data type
	 *      having fixed size.
	 *      
	 *    # contains duplicate data.
	 *    # data stored on the basis of indices(index starts from 0) 
	 *    # random access
	 *    # contiguous memory allocation.
	 *     
	 *    # syntax :
	 *    
	 *      data_type   array_name[]    = new  data_type[size];
	 *      
	 *      e.g.
	 *      
	 *         float  price[]  = new float[500];
	 *         String  []name  = new  String[50];
	 *         int[]  salary   = new   int[5000];
	 *         
	 *         
	 *         Employee  emps[]   = new  Employee[5000];
	 *         Book   books[]     = new  Book[250];
	 *      
	 */

}
